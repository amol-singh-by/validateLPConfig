# config-repo/lpa/lpa-secure.yaml

# Bulk Notification
bulkNotification:    
  https:
    tls:
      keyStore:
        password-secure: "changeme"
        key:
          password-secure: "changeme"
      trustStore:
        password-secure: "changeme"        
  sftp:
    password-secure: ""
    username-secure: ""
  ftp:
    password-secure: ""
    username-secure: ""

# JMS
jms:
  queue.password-secure: "admin"
  queue.user-secure: "admin"

# Can-model Service
canmodel:
  https:
    tls:
      keyStore:
        password-secure: "changeme"
        key:
          password-secure: "changeme"
      trustStore:
        password-secure: "changeme"

# LPA Server properties
server:
  https:
    tls:
      keyStore:
        password-secure: "changeme"
        key:
          password-secure: "changeme"
      trustStore:
        password-secure: "changeme"

# LP API HTTP Endpoint Config      
http:
  by:
    tenant-secure: "tenant-123"
    ingestion:
      password-secure: "password"
      username-secure: "userName"
    
# Message Store
messageStore:
  storeClientId-secure: "xx"
  storeClientSecret-secure: "xx"
  https:
    tls:
      keyStore:
        password-secure: "changeme"
        key:
          password-secure: "changeme"
      trustStore:
        password-secure: "changeme"

# Message Support
vts:
  client.id-secure: "xx"
  client.secret-secure: "xx"
  https:
    tls:
      keyStore:
        password-secure: "changeme"
        key:
          password-secure: "changeme"
      trustStore:
        password-secure: "changeme"

srs:
  client.id-secure: "xx"
  client.secret-secure: "xx"
  https:
    tls:
      keyStore:
        password-secure: "changeme"
        key:
          password-secure: "changeme"
      trustStore:
        password-secure: "changeme"